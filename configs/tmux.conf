# 0 is too far from ` ;)
set -g base-index 1

# Automatically set window title
set-window-option -g automatic-rename on
set-option -g set-titles on

#set -g default-terminal "screen-256color"
set -g status-keys vi
set -g history-limit 10000
setw -g mode-keys vi
setw -g mode-mouse on
set-option -g mouse-select-pane on

#Activity monitoring
setw -g monitor-activity on
set -g visual-activity on

# window
bind -n C-t new-window
bind -n C-n next-window
bind -n C-| split-window -h
bind -n C-s split-window -v

# Vim style pane selection
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# No delay for escape key press
set -sg escape-time 0

# Reload tmux config
bind r source-file ~/.tmux.conf

#### COLOUR (Solarized 256)
# default statusbar colors
set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg colour244 #base0
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg colour166 #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg colour235 #base02
set-option -g pane-active-border-fg colour240 #base01

# message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green


#### COLOUR (Solarized dark)
# default statusbar colors
#set-option -g status-bg colour235 #base02
#set-option -g status-fg colour130 #yellow
#set-option -g status-attr default

## default window title colors
#set-window-option -g window-status-fg colour33 #base0
#set-window-option -g window-status-bg default
##set-window-option -g window-status-attr dim

## active window title colors
#set-window-option -g window-status-current-fg colour196 #orange
#set-window-option -g window-status-current-bg default
##set-window-option -g window-status-current-attr bright

## pane border
#set-option -g pane-border-fg colour235 #base02
#set-option -g pane-active-border-fg colour46 #base01

## message text
#set-option -g message-bg colour235 #base02
#set-option -g message-fg colour196 #orange

## pane number display
#set-option -g display-panes-active-colour colour20 #blue
#set-option -g display-panes-colour colour196 #orange

## clock
#set-window-option -g clock-mode-colour colour40 #green


#### COLOUR (Solarized light)
# default statusbar colors
#set-option -g status-bg colour231 #base2
#set-option -g status-fg colour130 #yellow
#set-option -g status-attr default

## default window title colors
#set-window-option -g window-status-fg colour180 #base00
#set-window-option -g window-status-bg default
##set-window-option -g window-status-attr dim

## active window title colors
#set-window-option -g window-status-current-fg colour196 #orange
#set-window-option -g window-status-current-bg default
##set-window-option -g window-status-current-attr bright

## pane border
#set-option -g pane-border-fg colour231 #base2
#set-option -g pane-active-border-fg colour051 #base1

## message text
#set-option -g message-bg colour231 #base2
#set-option -g message-fg colour196 #orange

## pane number display
#set-option -g display-panes-active-colour colour20 #blue
#set-option -g display-panes-colour colour196 #orange

## clock
#set-window-option -g clock-mode-colour colour40 #green

# utf8
setw -g utf8 on
set-option -g status-utf8 on
setw -g window-status-current-format "|#I:#W|"

# status bar
set-option -g status on
set-option -g status-interval 5
set-option -g status-right-length 120
set -g status-right '\
E0 \
R:#(tx1=`cat /sys/class/net/eth0/statistics/rx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/eth0/statistics/rx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) \
T:#(tx1=`cat /sys/class/net/eth0/statistics/tx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/eth0/statistics/tx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) | \
W0 \
R:#(tx1=`cat /sys/class/net/wlan0/statistics/rx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/wlan0/statistics/rx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) \
T:#(tx1=`cat /sys/class/net/wlan0/statistics/tx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/wlan0/statistics/tx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) | \
LO \
R:#(tx1=`cat /sys/class/net/lo/statistics/rx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/lo/statistics/rx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) \
T:#(tx1=`cat /sys/class/net/lo/statistics/tx_bytes`\
         && sleep 1 && tx2=`cat /sys/class/net/lo/statistics/tx_bytes` \
         && echo "scale=2;\($tx2-$tx1\)/1024"|bc -l) | \
L#(uptime | tr -s " " | cut -d "e" -f4-) | \
MU:#(mt=`free -m | grep "Mem:" | tr -s " " | cut -d " " -f2`\
     && mu=`free -m | grep "Mem:" | tr -s " " | cut -d " " -f3`\
     && echo "scale=2;$mu/$mt" |bc -l)' #| \
#(date +"%a %b %d %H:%M")'

